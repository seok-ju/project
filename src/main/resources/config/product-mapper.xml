<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ProductDAO">
	<select id="cnt" resultType="int">
		select count(*) from PRODUCT where FLAG = 1
	</select>
	<select id="list" resultType="ProductVO">
		select * from PRODUCT where FLAG = 1 order by PRONUM desc
	</select>
	<select id="listPop" resultType="ProductVO">
		select * from PRODUCT where FLAG = 1 order by PROSELL desc
	</select>
	<select id="listOld" resultType="ProductVO">
		select * from PRODUCT where FLAG = 1 order by PRONUM
	</select>
	
	<select id="cnt2" resultType="int">
		select count(*) from PRODUCT where FLAG = 1 and PRODIV = '키링'
	</select>
	<select id="list2" resultType="ProductVO">
		select * from PRODUCT where FLAG = 1 and PRODIV = '키링' order by PRONUM desc
	</select>
	<select id="listPop2" resultType="ProductVO">
		select * from PRODUCT where FLAG = 1 and PRODIV = '키링' order by PROSELL desc
	</select>
	<select id="listOld2" resultType="ProductVO">
		select * from PRODUCT where FLAG = 1 and PRODIV = '키링' order by PRONUM
	</select>
	
	<select id="cnt3" resultType="int">
		select count(*) from PRODUCT where FLAG = 1 and PRODIV = '케이스'
	</select>
	<select id="list3" resultType="ProductVO">
		select * from PRODUCT where FLAG = 1 and PRODIV = '케이스' order by PRONUM desc
	</select>
	<select id="listPop3" resultType="ProductVO">
		select * from PRODUCT where FLAG = 1 and PRODIV = '케이스' order by PROSELL desc
	</select>
	<select id="listOld3" resultType="ProductVO">
		select * from PRODUCT where FLAG = 1 and PRODIV = '케이스' order by PRONUM
	</select>
	
	<select id="select" parameterType="int" resultType="ProductVO">
		select * from PRODUCT where PRONUM = #{PRONUM}	
	</select>
	
	<insert id="insert" parameterType="ProductVO">
		insert into PRODUCT 
		values(PRODUCT_SEQ.nextval, #{proName}, #{proPrice}, #{proContent}, #{proImg}, #{proTitle}, #{proStock}, 0, 1, #{proDiv}, #{proImg2})
	</insert>
	
	<update id="update" parameterType="ProductVO">
		update PRODUCT set PRONAME = #{PRONAME}, PROCONTENT = #{PROCONTENT},
		PROIMG = #{PROIMG}, PROTITLE = #{PROTITLE}, PROSTOCK = #{PROSTOCK} where PRONUM = #{PRONUM}
	</update>
	
	<delete id="delete" parameterType="int">
		delete from PRODUCT where PRONUM = #{PRONUM}
	</delete>
</mapper>
